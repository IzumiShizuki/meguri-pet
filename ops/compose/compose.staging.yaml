name: meguri-staging

services:
  migration:
    pull_policy: ${MEGURI_IMAGE_PULL_POLICY:-always}

  core:
    pull_policy: ${MEGURI_IMAGE_PULL_POLICY:-always}
    ports:
      - 127.0.0.1:${MEGURI_CORE_PORT:-18080}:8000
