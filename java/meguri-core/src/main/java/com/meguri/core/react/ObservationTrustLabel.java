package com.meguri.core.react;

/** Capability and child-agent observations never become authority input. */
public enum ObservationTrustLabel {
    UNTRUSTED_CAPABILITY_RESULT,
    UNTRUSTED_EXTERNAL_SKILL,
    UNTRUSTED_AGENT_RESULT
}
