# Website client demo

Start the local core on `127.0.0.1:8000`, then run:

```powershell
D:\environment\nodejs\runtime\node-v24.17.0-win-x64\node.exe src\demo.ts
```

This headless demo exercises the same website adapter intended for a browser UI. It keeps identity injection outside chat input and uses only in-memory storage.
