# Meguri text-model experiment card

- Task IDs:
- Experiment ID:
- Base model repo and revision:
- Tokenizer revision:
- Dataset ID / source build ID:
- Dataset Manifest SHA-256:
- Prompt / Response Schema / chat-template SHA-256:
- Framework and CUDA versions:
- Seed and LoRA configuration:
- Peak VRAM and training duration:
- Best validation checkpoint and selection reason:
- Last resumable checkpoint:
- Validation metrics:
- Locked-eval metrics (evaluation only):
- L0 and Provider deltas:
- Memorization and safety findings:
- Adapter SHA-256:
- Staging recommendation:
- Rollback model:
- Known risks and required human review:

Locked eval must never appear in checkpoint-selection evidence. A completed
card must link the raw-output hashes, not copy licensed source dialogue.
